// const express = require('express');
// const { home } = require('../controller/controllers');
// const router = express.Router();

// router.get('/', home);

// module.export = router;

// https://rickandmortyapi.com/api/character/?page=${pageNumber}&name=${search}

// http://localhost:8000/product/?name=${search}

// test('get by filter', async () => {
//     const res = await getCharacters({ name: 'Rick', status: 'Alive' })

//     res.data.results.forEach((item) => {
//       expect(item.name.includes('Rick')).toBe(true)
//       expect(item.status).toBe('Alive')
//     })
//   })